"use client";

import { supabaseStorageURL, moneyFormatBRL } from "@/ultils/ultils";
import { useState } from "react";
import { buttonClasses } from "@/styles/preset";

type Props = {
  order: any[];
};


export default function OrderString({ order }: Props) {

  if (order.length === 0) {

  } else {

  }
}
